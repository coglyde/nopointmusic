// Items shown in the cursor trail when hovering the vinyl.
// Drop .mp4 / .webm files into `public/clips/` and add their paths here —
// each will autoplay+loop+muted as a trail particle.
// PNG/JPG paths fall back to a quietly-spinning image.
export const TRAIL_ITEMS: readonly string[] = ["/deck/vinyl.png"];
